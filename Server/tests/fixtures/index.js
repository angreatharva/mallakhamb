/**
 * Test Fixtures Index
 *
 * Re-exports all factory functions for convenient importing.
 *
 * Usage:
 *   const { buildPlayer, buildCompetition } = require('../fixtures');
 */

module.exports = require('./factories');
